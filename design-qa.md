# 预约开单侧栏验收

final result: passed

## 最新调整：排客顺序与15分钟间隔

本节替代下方旧验收中有关老师ID、床位检查、床位按钮、时间刷新提示及20分钟时间段的描述。

- 已删除用户标出的老师ID、床位检查，以及日期刷新提示和整块床位时间按钮；同时删除对应旧样式、事件与函数引用。
- 表单改为纵向排列：客户类型 → 排客日期 → 排客时间 → 排客备注。选择客户类型后启用日期，选择日期后启用时间。客户类型变更会清空日期和时间，日期变更会清空时间。
- 原型未配置营业时段，提供00:00至23:45共96个选项。浏览器核验所有相邻选项差值均为15分钟，包括10:00、10:15、10:30、10:45。
- 浏览器验证老客、2026-09-23、10:15排客成功；变更客户类型、日期后的重选逻辑通过。被删除控件数量为0，控制台无error，脚本语法及diff空白检查通过。
- 最新截图：`outputs/booking-sidebar/schedule-revised.png`，1440×900、密度1，排客区单列及底部按钮显示完整。字体、配色、圆角沿用上一版。
- 本次是用户指定删除与交互修改，旧参考图中对应内容不再作为视觉目标。无剩余阻塞项。

本次范围：`weixin/聊天pc.html` 的预约开单 Tab，按用户图1接图2的顺序替换内容。未改其他 Tab 的内容及聊天业务。

## 参考与证据

- 图1：`outputs/booking-sidebar/reference-top.png`（311 × 696）。
- 图2：`outputs/booking-sidebar/reference-bottom.png`（308 × 580）。
- 页面：`http://127.0.0.1:8765/weixin/聊天pc.html`。
- 完整桌面截图：`outputs/booking-sidebar/desktop.png`、`desktop-bottom.png`。
- 对照截图：`outputs/booking-sidebar/comparison-top.png`、`comparison-bottom.png`。
- 视口1440 × 900，devicePixelRatio=1；保留原页面320px侧栏，内容宽291px。上半截图320 × 700，下半320 × 596，均按CSS像素裁剪，未放大参考图。
- 状态：预约开单已选中，订单待排客；上半为初始展示，下半为刷新后的时间提示。当前客户名称、设备继续跟随原聊天选择，因此与参考客户姓名不同。
- 浏览器截图有采样模糊，细字以可访问性文本和DOM尺寸补充核对；本次验收为内容、排版和交互，不宣称逐像素一致。

## 视觉核对

- 字体与字号：沿用原页面Inter及中文系统字体；标题14px，卡片标题/正文11–12px，说明10–11px，标签和金额均可完整显示。
- 间距与布局：客户卡、蓝色订单提示、平台校验、订单信息、订单项目、排客表单、底部三按钮依次排列；白色圆角卡、两列表单、三列时间选择匹配参考层级。保留原侧栏宽度和滚动容器，下半表单略高于参考，属于既有页面尺寸适配。
- 颜色：浅灰背景、白卡、蓝色提示及待排客标签、绿色校验与充值、紫色排客按钮和时间选中态均已对应。
- 图像与图标：参考区域没有照片等位图资产；沿用页面Phosphor图标。首轮发现calendar-clock无图标，已替换为现有库calendar-check，复核图标已渲染。
- 文案与内容：两张图中的所有卡片、字段、时间段、操作均已包含；订单样例采用参考值，当前客户保持原聊天上下文。
- 对照图已同时显示参考与实际；上半、下半分别作为局部放大核对证据，无需另建更小区域截图。

## 交互验收

- 日期改为2026-09-22后，刷新提示更新为对应日期。
- 选择10:20后，下拉框及时间按钮选中态同步。
- 点击立即排客，订单变为已排客，按钮变为确认改约，显示日期和时间结果。
- 底部修改可打开订单表单，预约金额改为20.00后正确显示。
- 项目修改可打开单价表单，改为399.00后单价与总价同步。
- 充值打开金额表单；确认100元后显示演示反馈，未请求接口或实际扣款。
- 刷新入口可操作，取消可关闭对应编辑表单。
- 1440px及1024px桌面宽度下，侧栏内容clientWidth与scrollWidth均为291，无横向溢出。
- 浏览器error日志为空；内联JavaScript语法检查和git diff --check通过。

## 迭代与结论

首轮发现排客图标缺失（P2），已改用现有图标库支持的日历图标，最终截图及图标伪元素复核通过。没有剩余P0/P1/P2问题。

保留差异：当前客户来自选中的聊天；表单控件采用现有浏览器日期与选择器；侧栏按原页面320px适配。按钮均为本地演示，不接真实平台服务。未扩展移动端整页布局。
